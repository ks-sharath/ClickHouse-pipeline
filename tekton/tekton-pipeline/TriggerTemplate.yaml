apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerTemplate
metadata:
  name: clickhouse-build-template
spec:
  params:
    - name: git-revision
      description: The git revision
      default: master
    - name: git-repo-url
      description: The git repository URL
      default: https://github.com/ks-sharath/ClickHouse-pipeline.git
  resourcetemplates:
    - apiVersion: tekton.dev/v1beta1
      kind: PipelineRun
      metadata:
        generateName: clickhouse-build-run-
      spec:
        pipelineRef:
          name: build-clickhouse-pipeline
        params:
          - name: repository-url
            value: $(params.git-repo-url)
          - name: source-dir
            value: $(params.git-revision)  # This might need to be adjusted based on your setup
        workspaces:
          - name: shared-workspace
            emptyDir: {}  # Using emptyDir for simplicity; use PVC in production
